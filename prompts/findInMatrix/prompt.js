/*
 * Given an M x N matrix, write a method to find an element in the matrix.
 * When the element is found, return an array of the coordinates.
 */
var findElement = function(matrix, target) {

};

module.exports = findElement;

