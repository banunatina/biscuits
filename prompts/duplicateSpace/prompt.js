/*
 * Given an array of positive integers 1 to N, where N + 1 is the array length,
 * assume that at least one of the integers occurs more than once.
 *
 * Write a function that finds an integer that appears more than once in the array.
 * The function must use constant space.
 *
 * EXTRA: The function must use linear time.
 *
 */

var findDuplicate = function(array) {

};
