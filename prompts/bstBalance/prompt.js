/*
 *
 * Implement a function that checks if a binary search tree is balanced.
 * In this case, a binary search tree is balanced if the heights of the two subtrees
 * of any node never differ by more than 1.
 *
 */

var isBSTBalanced = function(tree) {

};

module.exports = isBSTBalanced;
