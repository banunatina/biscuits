// This repo comes with mocha and chai.
var expect = require('chai').expect;
// Replace nameOfFunction and nameOfPrompt below
var nameOfFunction = require('../prompts/nameOfPrompt/solution.js');

// Let the testing begin
describe('nameOfPrompt', function() {
  // beforeEach (or before) hook if needed
  beforeEach(function() {
    // Something to do before every test
  });

  it('should do something', function(done) {
    // Assertions  
  });
});
